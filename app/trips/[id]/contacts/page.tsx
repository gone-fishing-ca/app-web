"use client";
import { ContactRound } from "lucide-react";
import { ModuleStub } from "@/components/stub";

export default function Page() {
  return (
    <ModuleStub icon={ContactRound} title="Contacts"
      blurb="Outfitters, local businesses, and emergency contacts. The data model is wired up — UI is next." />
  );
}
